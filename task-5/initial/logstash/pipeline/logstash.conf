input {
	beats {
		port => 5044
		host => "0.0.0.0"
	}
}

output {
	elasticsearch {
		hosts => ["http://elasticsearch:9200"]
		manage_template => false
		index => "batch-processing-%{+YYYY.MM.dd}"
	}

	stdout { codec => rubydebug }
}
